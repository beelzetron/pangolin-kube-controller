// Package metrics_prometheus provides Prometheus metrics collection.
//
// Note: the package name includes an underscore for consistency with the
// sibling package `metrics_otel`. This is an intentional convention used in
// this repository to clearly separate Prometheus- and OpenTelemetry-based
// metric providers; keep imports using the package name `metrics_prometheus`.
package metrics_prometheus
