// Package labels resolves and verifies the Traefik instance label
// (key/value) applied to all resources managed by the controller. It supports
// autodetection from IngressClass and periodic verification.
package labels
