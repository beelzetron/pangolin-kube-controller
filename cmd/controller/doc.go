// Command pangolin-kube-controller runs the Pangolin Kubernetes Controller.
// It loads configuration from environment and orchestrates the reconcile loop
// that fetches Traefik dynamic configuration and applies Traefik CRDs.
package main
